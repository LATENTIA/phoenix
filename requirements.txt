pandas>=2.0
requests>=2.31
flask>=3.0

# Security hardening (added for AWS-ready deploy):
flask-wtf>=1.2          # CSRFProtect on state-changing routes
flask-limiter>=3.5      # Rate limiting on /run/* and /report/*
flask-talisman>=1.1     # Security headers (CSP, X-Frame-Options, etc.)

# Optional, only used when PHOENIX_SECRETS_BACKEND=aws:
boto3>=1.34             # AWS Secrets Manager client for Flex tokens

# Production WSGI server. The Dockerfile uses this; local Windows dev keeps
# using Flask's built-in dev server via `python app.py` since gunicorn is
# POSIX-only (depends on os.fork). `pip install` will succeed on Windows but
# the binary won't run there.
gunicorn>=22.0
